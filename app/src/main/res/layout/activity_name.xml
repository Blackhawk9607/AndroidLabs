<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nameLayouts"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NameActivity">

    <!-- TextView for "Welcome!" -->
    <TextView
        android:id="@+id/NameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/NameTextview"
        android:textSize="18sp"
        android:textColor="#000000"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="40dp"
        android:layout_marginStart="16dp"/>

    <!-- LinearLayout to contain both buttons -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/NameTextView"
        app:layout_constraintStart_toStartOf="parent">

        <!-- Button for "DON'T CALL ME THAT" -->
        <Button
            android:id="@+id/NameButton1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/NameButtonText1"
            android:textAllCaps="true"
            style="?android:attr/buttonBarButtonStyle"
            android:backgroundTint="#FF0000"
            android:textColor="#000000"
            android:layout_marginEnd="16dp"/>

        <!-- Button for "THANK YOU" -->
        <Button
            android:id="@+id/NameButton2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/NameButtonText2"
            android:textAllCaps="true"
            style="?android:attr/buttonBarButtonStyle"
            android:backgroundTint="#008000"
            android:textColor="#FFFFFF"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
